{"name":"Fix invoice amount paid calculation","on":{"push":{"branches":["main"]}},"permissions":{"contents":"write"},"jobs":{"patch":{"runs-on":"ubuntu-latest","steps":[{"uses":"actions/checkout@v4"},{"name":"Fix InvoiceDetail calculation","run":"python3 - <<'PY'\nfrom pathlib import Path\np=Path('qvbit-site/crm/src/main.jsx')\ns=p.read_text(encoding='utf-8')\nold_save=\"\"\"    if (amountPaid > total && form.status !== 'void') {\n      setError('Amount paid cannot be greater than the invoice total.')\n      setSaving(false)\n      return\n    }\n\n    const payload = {\n      invoice_number: form.invoice_number.trim() || null,\n      customer_id: form.customer_id,\n      job_id: form.job_id || null,\n      status: form.status,\n\"\"\"\nnew_save=\"\"\"    if (amountPaid > total && form.status !== 'void') {\n      setError('Amount paid cannot be greater than the invoice total.')\n      setSaving(false)\n      return\n    }\n\n    const calculatedStatus = form.status === 'void'\n      ? 'void'\n      : (amountPaid >= total && total > 0)\n        ? 'paid'\n        : amountPaid > 0\n          ? 'partial'\n          : ['paid', 'partial'].includes(form.status)\n            ? 'sent'\n            : form.status\n\n    const payload = {\n      invoice_number: form.invoice_number.trim() || null,\n      customer_id: form.customer_id,\n      job_id: form.job_id || null,\n      status: calculatedStatus,\n\"\"\"\nif old_save not in s: raise SystemExit('save block not found')\ns=s.replace(old_save,new_save,1)\nmarker=\"  async function remove() {\"\ninsertion=\"\"\"  const liveTotal = Number(form.total || 0)\n  const liveAmountPaid = Number(form.amount_paid || 0)\n  const liveBalanceDue = Math.max(liveTotal - liveAmountPaid, 0)\n  const liveInvoiceStatus = form.status === 'void'\n    ? 'void'\n    : (liveAmountPaid >= liveTotal && liveTotal > 0)\n      ? 'paid'\n      : liveAmountPaid > 0\n        ? 'partial'\n        : ['paid', 'partial'].includes(form.status)\n          ? 'sent'\n          : form.status\n\n\"\"\"\nif 'const liveBalanceDue' not in s:\n    if marker not in s: raise SystemExit('remove marker not found')\n    s=s.replace(marker,insertion+marker,1)\np.write_text(s,encoding='utf-8')\nprint('Invoice amount paid calculation fixed.')\nPY"},{"name":"Build CRM","working-directory":"qvbit-site/crm","run":"npm install --no-audit --no-fund && npm run build"},{"name":"Commit and remove workflow","run":"git config user.name 'github-actions[bot]' && git config user.email '41898282+github-actions[bot]@users.noreply.github.com' && git add qvbit-site/crm/src/main.jsx && git diff --cached --quiet || git commit -m 'Fix invoice amount paid calculation' && git rm .github/workflows/fix-invoice-amount-paid-calculation.yml && git commit -m 'Remove invoice amount paid calculation workflow' && git push"}]}}}